# Additional.iso
ISO_DEVICE=/dev/sr0
ISO_MOUNT=/mnt
WEB_SOURCE_DIR=web

# Apache
WEB_ROOT=/var/www/html
WEB_PORT=80

# MariaDB
DB_HOST=localhost
DB_NAME=webdb
DB_USER=webc
DB_PASSWORD=P@ssw0rd

# Recreate the application database before importing dump.sql.
RESET_DATABASE=yes
